'use strict'
{
   let z = +prompt('Введите число 1');
   let y = +prompt('Введите число 2');
   
const calMax = (a, b) => {
   if(z >= y) {
      console.log(a);
   } else if (z <= y) {
      console.log(b);
   } else {
   'Вы ввели неверные данные';
};
};
calMax(a,b)
};